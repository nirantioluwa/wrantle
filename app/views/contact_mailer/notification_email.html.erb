<div class="mb-4">
  <h1 class="h1 color-fg-default mb-2">New Contact Request</h1>
  <span class="Label Label--success mr-1"><%= @contact.contact_type.humanize %></span>
  <% if @contact.contact_type == "schedule_chat" %>
    <span class="Label Label--success">Meeting Request</span>
  <% end %>
</div>

<div class="mb-4">
  <h2 class="h3 color-fg-default mb-2">Contact Information</h2>
  <div class="color-bg-default p-3">
    <p><strong>Name:</strong> <%= @contact.name %></p>
    <p><strong>Email:</strong> <%= @contact.email %></p>
    <p><strong>Phone:</strong> <%= @contact.phone %></p>
    
    <% if @contact.contact_type == "schedule_chat" %>
      <p><strong>Preferred Date:</strong> <%= @contact.preferred_date.strftime("%B %d, %Y") %></p>
      <p><strong>Preferred Time:</strong> <%= @contact.preferred_time.strftime("%I:%M %p") %></p>
    <% end %>
  </div>
</div>

<div class="mb-4">
  <h2 class="h3 color-fg-default mb-2">Message</h2>
  <div class="color-bg-default p-3">
    <%= simple_format(@contact.message) %>
  </div>
</div>

<p class="f4 color-fg-muted">
  <% if @contact.contact_type == "schedule_chat" %>
    Please review the meeting request and confirm the schedule with the client.
  <% elsif @contact.contact_type == "request_info" %>
    Please prepare the requested information and respond to the client.
  <% else %>
    Please review their needs and prepare a detailed assessment.
  <% end %>
</p>

<div class="f4 color-fg-muted">
  Reply directly to this email to respond to <%= @contact.name %>.
</div>

<% content_for :footer do %>
  <p class="color-fg-muted f6">This is an automated notification from the Wrantle contact system.</p>
<% end %>