<%# About Navigation %>
<%= render(Primer::Box.new(p: 4)) do %>
  <%= render(Primer::Alpha::ActionBar.new(color: :success, classes: "about-action-bar")) do |component| %>
    <%= component.with_item_icon_button(
      bg: current_page?(root_path) ? :success : nil,
      p: 2,
      icon: "home",
      label: "Home",
      tag: :a,
      href: root_path,
      classes: "about-icon-button"
    ) %>
    <%= component.with_item_icon_button(
      bg: current_page?(services_path) ? :success : nil,
      p: 2,
      icon: "apps",
      label: "Services",
      tag: :a,
      href: services_path,
      classes: "about-icon-button"
    ) %>
    <%= component.with_item_icon_button(
      bg: current_page?(writings_path) ? :success : nil,
      p: 2,
      icon: "book",
      label: "Writings",
      tag: :a,
      href: writings_path,
      classes: "about-icon-button"
    ) %>
    <%= component.with_item_icon_button(
      bg: current_page?(about_path) ? :success : nil,
      p: 2,
      icon: "info",
      label: "About",
      tag: :a,
      href: about_path,
      classes: "about-icon-button"
    ) %>
    <%= component.with_item_divider %>
    <%= component.with_item_icon_button(
      bg: current_page?(single_north_star_path) ? :success : nil,
      p: 2,
      icon: "north-star",
      label: "A Single North Star",
      tag: :a,
      href: single_north_star_path,
      classes: "about-icon-button"
    ) %>
    <%= component.with_item_icon_button(
      bg: current_page?(live_impact_thrive_path) ? :success : nil,
      p: 2,
      icon: "stack",
      label: "Live, Impact & Thrive",
      tag: :a,
      href: live_impact_thrive_path,
      classes: "about-icon-button"
    ) %>
    <%= component.with_item_icon_button(
      bg: current_page?(impact_beyond_us_path) ? :success : nil,
      p: 2,
      icon: "infinity",
      label: "Impact Beyond Us",
      tag: :a,
      href: impact_beyond_us_path,
      classes: "about-icon-button"
    ) %>
    <%= component.with_item_icon_button(
      bg: current_page?(modern_innovator_challenge_path) ? :success : nil,
      p: 2,
      icon: "light-bulb",
      label: "The Innovator's Challenge",
      tag: :a,
      href: modern_innovator_challenge_path,
      classes: "about-icon-button"
    ) %>
    <%= component.with_item_icon_button(
      bg: current_page?(accelerating_business_growth_path) ? :success : nil,
      p: 2,
      icon: "rocket",
      label: "Accelerate Growth",
      tag: :a,
      href: accelerating_business_growth_path,
      classes: "about-icon-button"
    ) %>
    <%= component.with_item_icon_button(
      bg: current_page?(building_for_tomorrow_path) ? :success : nil,
      p: 2,
      icon: "telescope",
      label: "Build Tomorrow",
      tag: :a,
      href: building_for_tomorrow_path,
      classes: "about-icon-button"
    ) %>
  <% end %>
<% end %>
