<%# SEO Meta Tags %>
<% content_for :title, "Expert Advisory for Technology and Innovative Ventures - Wrantle" %>
<% content_for :meta_description, "Wrantle provides expert advisory services for technology and innovative ventures. We help teams build and scale their ventures through our expert services." %>
<% content_for :og_title, "Expert Advisory for Technology and Innovative Ventures - Wrantle" %>
<% content_for :og_description, "Wrantle provides expert advisory services for technology and innovative ventures. We help teams build and scale their ventures through our expert services." %>

<%# Main Navigation %>
<%= render "shared/main_sections_navigation" %>

<%# Hero Section %>
<%= render(Primer::Box.new(p: 0, mb: 6)) do %>
  <%= render "shared/hero" %>
<% end %>

<%# Stats Section %>
<%= render(Primer::Box.new(p: 0, mb: 6)) do %>
  <%= render "shared/stats_box" %>
<% end %>

<%# Services Section %>
<%= render(Primer::Box.new(p: 2, mb: 6, mt: 6)) do %>
<%= render(Primer::Box.new(p: 2, mb: 6, mt: 6, bg: :subtle)) do %>
  <%= render "shared/services_box" %>
  <% end %>
<% end %>

<%# Principles Section formerly Features %>
<%= render(Primer::Box.new(p: 0, mb: 6, mt: 6)) do %>
  <%= render "shared/principles_box" %>
<% end %>

<%# Contact Section %>
<%= render "shared/contact_box",
  title: "How can we help?",
  subtitle: "Let's talk about how we can help you scale your structure" %>