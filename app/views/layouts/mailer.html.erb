<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <style>
      /* Base styles */
      body { 
        margin: 0; 
        padding: 0; 
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
        color: #24292f;
        background-color: #ffffff;
      }
      
      /* Layout */
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { padding: 8px; text-align: left; }
      .content { padding: 24px; }
      
      /* Typography */
      .h1 { font-size: 20px; font-weight: 600; margin-bottom: 16px; }
      .h3 { font-size: 16px; font-weight: 600; margin-bottom: 12px; }
      .f4 { font-size: 16px; line-height: 1.5; }
      .f6 { font-size: 12px; }
      
      /* Colors */
      .color-fg-default { color: #24292f; }
      .color-fg-muted { color: #57606a; }
      
      /* Spacing */
      .mb-4 { margin-bottom: 24px; }
      .mb-2 { margin-bottom: 12px; }
      .mr-1 { margin-right: 8px; }
      .p-3 { padding: 16px; }
      
      /* Logo */
      .logo {
        font-size: 32px;
        font-weight: bold;
        color: #238636;
        text-decoration: none;
        font-family: ui-monospace, SFMono-Regular, SF Mono, Menlo, monospace;
      }
      
      /* Footer */
      .footer {
        text-align: center;
        padding: 16px;
        color: #57606a;
        font-size: 14px;
        border-top: 1px solid #d0d7de;
        margin-top: 32px;
      }

      /* Signature */
      .signature {
        margin-top: 32px;
        color: #24292f;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <div class="logo">Wrantle</div>
      </div>

      <div class="content">
        <%= yield %>
      </div>

      <div class="footer">
        <%= yield :footer %>
      </div>
    </div>
  </body>
</html>
