<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <style>
      /* Base styles */
      body { margin: 0; padding: 0; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif; }
      
      /* Layout */
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { padding: 16px; text-align: left; }
      .content { background-color: #238636; padding: 24px; color: #ffffff; }
      
      /* Typography */
      .h1 { font-size: 24px; font-weight: 600; margin-bottom: 16px; color: inherit; }
      .h3 { font-size: 18px; font-weight: 600; margin-bottom: 12px; color: inherit; }
      .f4 { font-size: 16px; line-height: 1.5; }
      .f6 { font-size: 12px; }
      
      /* Colors */
      .color-fg-default { color: inherit; }
      .color-fg-muted { color: #8b949e; }
      .color-bg-default { background-color: #ffffff; color: #24292f; }
      
      /* Spacing */
      .mb-4 { margin-bottom: 24px; }
      .mb-2 { margin-bottom: 12px; }
      .mr-1 { margin-right: 8px; }
      .p-3 { padding: 16px; }
      .px-4 { padding-left: 24px; padding-right: 24px; }
      .py-2 { padding-top: 12px; padding-bottom: 12px; }
      
      /* Components */
      .btn {
        display: inline-block;
        text-decoration: none;
        background-color: #ffffff;
        color: #24292f;
        border: 0;
        margin-top: 16px;
      }
      
      .Label {
        display: inline-block;
        padding: 4px 8px;
        font-size: 14px;
        font-weight: 500;
        background-color: #ffffff;
        color: #24292f;
      }
      
      /* Logo */
      .logo {
        font-size: 32px;
        font-weight: bold;
        color: #238636;
        text-decoration: none;
        font-family: ui-monospace, SFMono-Regular, SF Mono, Menlo, monospace;
      }
      
      /* Footer */
      .footer {
        text-align: center;
        padding: 16px;
        color: #57606a;
        font-size: 14px;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <div class="logo">W</div>
      </div>

      <div class="content">
        <%= yield %>
      </div>

      <div class="footer">
        <%= yield :footer %>
      </div>
    </div>
  </body>
</html>
